# sdkmanager packages installed into $ANDROID_SDK_ROOT on first run, and
# whenever this file changes. platform-tools and emulator have no version
# selector - sdkmanager always installs the current stable channel build.
platform-tools
emulator
platforms;android-34
build-tools;36.0.0
system-images;android-34;google_apis_playstore;x86_64
